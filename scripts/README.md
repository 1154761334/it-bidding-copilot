# 脚本目录说明

当前 `scripts/` 按职责分为三类：

- `ops/`: 初始化、迁移、盘点、运维辅助
- `seed/`: 演示数据与结构化种子
- `verify/`: 真实链路联调与验证

使用原则：

1. 一次性实验脚本不要再直接丢在 `scripts/` 根目录
2. 需要长期保留的脚本必须进入上述三类之一
3. 新增验证脚本优先放到 `scripts/verify/`
4. 新增演示数据脚本优先放到 `scripts/seed/`
